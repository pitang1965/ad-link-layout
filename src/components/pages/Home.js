import React, { Fragment } from 'react';
import Preview from '../preview/Preview';
import Editor from '../editor/Editor';
const Home = () => (
  <Fragment>
    <Preview />
    <Editor />
  </Fragment>
);

export default Home;
